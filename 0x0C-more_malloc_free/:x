#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
  * malloc_checked - function to allocate my created memory
  * @b: the size to be allocated
  *
  * Return: hakuna kitu
  */
void *malloc_checked(unsigned int b)
{
	void *x;

	x = malloc(b);

	if (x == '\0')
		exit(98);

	return (x);
}
